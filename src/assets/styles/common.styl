html {
  font-size: 100px;
  background-color #ededed;
}

.container {
  display: flex;
  flex-direction: column;
}

.scroll-wrapper {
    display: flex;
    flex:1;
    flex-direction: column;
    overflow hidden
}